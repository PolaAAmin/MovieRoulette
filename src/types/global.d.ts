// Ambient declarations for the MovieRoulette app.
//
// Add module-augmenting types here (e.g. extending `import.meta.env`,
// custom global variables, or third-party shims). Per-file ambient
// types should live next to their declaring module instead.

// CSS side-effect imports (Vite handles the actual loading at runtime).
declare module "*.css" {}
